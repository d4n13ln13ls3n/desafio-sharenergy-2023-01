import React from 'react';

export default function backToAllButtonsButton(props) {
  return (
    <button
      id="back-all-buttons-button"
      onClick={props.onClick}
      >
      Voltar
    </button>

  )
}